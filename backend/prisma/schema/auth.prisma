model User {
  id        Int      @id @default(autoincrement())
  email     String   @unique
  username  String?
  hp        Int      @default(100)
  inventory InventoryItem[]
  createdAt DateTime @default(now())
}